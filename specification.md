# Horizon 進捗管理システム 仕様書

## はじめに

### 概要
これは主に基礎班員の学習進捗を管理するためのシステムです。

ほとんどの機能が自動的に実行される設計になっているため、管理者は特別な操作をする必要はありません。
また、処理に関わる設定なども用意されたシートを通じて設定するため、コードを頻繁に変更する負担を軽減します。

### 目的
このシステムの主な目的は**基礎班員の進捗を管理すること**です。

また、副次的な目的として以下の３点があげられます。

1. **基礎班運営担当者の負担軽減**

	- このシステムは部員の進捗の収集から一覧化までを一貫して自動化するため、担当者の負担を軽減します。
  
2. **進捗情報の一元管理** 

   - 単一のスプレッドから進捗管理に必要な情報をすべて閲覧することができるため、引き継ぎ等の手間を削減できます。
  
3. **他システムとの進捗情報の共有**

	- 進捗状況をスプレッドシートに保存することで、他のシステムからの情報へのアクセスを容易にし連携を促進します。
	- 例として、すでにHorizon Atlasによる各部員の進捗状況確認機能が実装されています。

### 開発背景
このシステムは2024年度基礎班運営で使用されていた旧進捗管理システムに存在した以下のような欠点を解消するために開発されました。

- 問題ごとに存在するフォームとスプレッドシート・GAS
  
	- 旧システム : 各問題にそれぞれフォーム、スプレッドシートとそれに紐づいたGASが存在し、管理の手間が膨大でした。
	- このシステムので改善 : 基礎班で取り扱うすべての問題を同一のフォームとスプレッドシートで管理できるようになります。

- 一部練習問題における進捗の一覧表示の未実装
	
	- 旧システム : Python基礎文法における進捗一覧機能は未実装でした。
	- このシステムので改善 : すべての問題において一元化された進捗一覧を自動的に作成・表示できるようになります。
  
- 各問題ごとのシステム開発の必要性
  
	- 旧システム : 各問題においてその問題に応じたフォームの準備やGASの開発が必要でした。
	- このシステムので改善 : すべての問題の進捗状況の管理を単一のフォーム・スプレッドシート・GASで行います。
  
- ドキュメントの未整備と複雑怪奇な運用方法

	- 旧システム : 上記のような理由から長期的な運用は難しい想定でした。
	- このシステムので改善 : システムの運用方法をシンプルにし、仕様書や利用方法などのドキュメントの整備を行うことにより長期的な運用保守をサポートします。

### 主な機能

* **フォーム記録シートへの情報追加**

	フォームから送信された情報を記録するシートに氏名と学舎を追加します。

* **進捗一覧シートの自動記録**

	フォームから送信された情報および名簿から取得したデータをもとに、`進捗一覧`シートに自動的に進捗を記録します。
	これは、進捗収集フォームが送信されたタイミングで自動的に実行されます。

* **進捗収集フォームの自動更新**

	進捗収集フォームについて以下の点を設定に応じて自動的に更新します。

    * 終了した問題の選択肢（`問題マスター`シートより情報取得）
    * 各セクションおよび質問のタイトル（`フォーム設定`シートより情報取得）

	それぞれの処理は、対応するシートが更新されたタイミングで自動的に実行されます。

* **進捗一覧シートの問題名の自動更新**

	進捗一覧シートの問題名を、`問題マスター`シートから自動的に更新します。
	これは、`問題マスター`シートが更新されたタイミングで自動的に実行されます。


### 使用技術

#### 実動作環境

- **Google Form**
  
	基礎班員が進捗を送信するために使用

- **Google Spreadsheet**

	進捗情報の蓄積や各設定を行うために使用

- **GAS (Google App Script)**
  
	Spreadsheetに紐づけて各種自動処理のロジックを実装するために使用

#### 開発環境

- **clasp**
  
	GASの開発をローカルエディター(VSCode)から行うために使用

- **@types/google-apps-script**
  
	GASの型定義ファイル。VSCodeのインテリセンス機能を有効化するために使用

- **git / GitHub**

	ソースコードのバージョン管理を行うために使用


## システム設計

### システム構成

このシステムは以下の３つで構成されています。

* **進捗収集フォーム:** 班員が自身の進捗状況を報告するために使用します。
* **管理用スプレッドシート:** 収集された進捗データや、システムの設定情報を格納します。
* **GAS（Google Apps Script）:** スプレッドシートとフォームを連携させ、データ処理や自動更新などのロジックを実行します。

### 進捗収集フォーム

カリキュラムに付随する問題・課題を終了した人が回答するGoogleフォームです。
以下にこのフォームに含まれるアイテムとその構造について記述します。

すべてのアイテムのタイトルは`フォーム設定`シートより設定可能です。
`フォーム設定`シートには各アイテムのIDも記録されています。このアイテムIDは原則変更してはなりません（後述）。

また、このシステムは以下のフォーム構造を前提とした設計となっています。

**セクション1（問題選択）**
* 完了した問題を選択する質問
  （例: Python - 練習問題1, Flet - 電卓 等）

	ここで「まとめ問題」カテゴリに分類される問題を選んだ場合、セクション2へ移動します。選んだのが「まとめ問題」でなかった場合、セクション3に移動します。

**セクション2（ファイルのアップロード）:**
* セクションヘッダ（PageBreakItem）
* プログラムファイルをアップロードする質問
  最大ファイル数を5としています。

このあとはセクション3へ移動します。

**セクション3（理解度）:**
* セクションヘッダ（PageBreakItem）
* 理解度を５段階（低: 1 高: 5）で回答する質問

	ここで理解度として「1」「2」「3」「4」を選ぶとセクション4に移動し、質問などが必須となります。「5」を選んだ場合セクション5に移動します。

**セクション4（フィードバック（必須））:**
* セクションヘッダ（PageBreakItem）
* 質問などのフィードバックを回答する質問

	このあとはフォームを送信します。

**セクション5（フィードバック（任意））:**
* セクションヘッダ（PageBreakItem）
* 質問などのフィードバックを回答する質問

	このあとはフォームを送信します。

### 管理用スプレッドシート

技術部員、主に基礎班運営担当者がこのシステムの管理のために使用するスプレッドシートです。
このスプレッドシートでは以下のことができます。

* フォームの回答の確認
* フォームの**セクション1（問題選択）**で表示する問題名とそのカテゴリ、問題IDの設定
* 部員名簿の確認
* 進捗一覧シートの閲覧
* GASロジックに関わる設定（各種シートのIDやデータを記録する列・行番号、エラー通知先メールアドレスなど）
* 進捗収集フォームに関わる設定（各アイテムのタイトルやIDなど）

このスプレッドシートは**フォームの回答を記録するシート**、**各問題を管理するシート**、**部員名簿**、**進捗一覧**、**GASロジックに関わる設定を行うシート**、**フォームに関わる設定を行うシート**からなります。各シートの名前は任意に変更が可能です（後述）。
以下に各シートの詳細を記述します。

#### フォームの回答を記録するシート :
Googleフォームから送信された全ての回答履歴を時系列で蓄積します。
項目名はフォーム設定によって変動するため一例です。

| 列   | 項目名             | 説明                                  | データ型      | 備考                     |
| :--- | :----------------- | :------------------------------------ | :------------ | :----------------------- |
| A    | タイムスタンプ     | フォーム回答日時 (自動記録)           | 日時          |                          |
| B    | メールアドレス     | 回答者のGoogleアカウント (自動記録)   | 文字列        | **先頭7文字が学籍番号**  |
| C    | 完了した問題名     | フォームで選択された問題名            | 文字列        |                          |
| D    | プログラムファイル | アップロードされたファイルのURLリスト | 文字列        | まとめ問題の場合のみ記録 |
| E    | 理解度             | 5段階評価の結果                       | 数値          |                          |
| F    | 質問など（必須）   | 自由記述                              | 文字列 (長文) | 理解度が1~4のとき        |
| G    | 質問など（任意）   | 自由記述                              | 文字列 (長文) | 理解度が5のときのみ      |
| H    | 氏名               | 回答者の氏名 (GASにより自動記録)      | 文字列        | 部員名簿シートより取得   |
| I    | 学舎               | 回答者の氏名 (GASにより自動記録)      | 文字列        | 部員名簿シートより取得   |


#### 各問題を管理するシート :
練習問題に関する情報を一元管理します。

| 列   | 項目名         | 説明                                 | データ型             | 例                                          | 備考                                                                  |
| :--- | :------------- | :----------------------------------- | :------------------- | :------------------------------------------ | :-------------------------------------------------------------------- |
| A    | 問題ID         | 問題のID。システム内部での識別キー。 | 文字列               | `python-prac1`, `python-summary`            | **ユニーク必須。空白は禁止**                                          |
| A    | 問題名         | 問題の名称・表示名。                 | 文字列               | `Python - 練習問題1`, `Python - まとめ問題` | ここを変更するとフォームの選択肢も更新 。シート内で一意である必要アリ |
| B    | 問題種類       | `練習問題` または `まとめ問題`       | 文字列               | `練習問題`, `まとめ問題`                    | フォームのセクション分岐等で使用                                      |
| C    | 現在有効フラグ | `TRUE`=有効, `FALSE`=無効            | **チェックボックス** | `TRUE`, `FALSE`                             | フォーム選択肢・進捗一覧列生成に影響                                  |
| D    | 表示順         | 表示時の並び順を制御するための数値   | 数値                 | `10`, `20`, `100`                           | フォーム選択肢・進捗一覧で小さい順に表示                              |

> [!CAUTION]
> 「問題名」は現在『[カテゴリ名] - [問題名]』という形式で設定しなければなりません。これはHorizon Atlasで進捗を表示する際の処理に起因します。
> 例: `Python - 練習問題1`, `Flet - 電卓`


#### 部員名簿 :
学籍番号と氏名の対応を管理します。

| 列   | 項目名     | 説明                                            | データ型 | 例          | 備考                         |
| :--- | :--------- | :---------------------------------------------- | :------- | :---------- | :--------------------------- |
| A    | 学籍番号   | 部員の一意な学籍番号（メールアドレス先頭7文字） | 文字列   | `25X0001`   | **ユニーク必須、正確性必須** |
| B    | 氏名       | 部員の氏名                                      | 文字列   | `山田 太郎` | **ユニーク推奨**             |
| C    | 学舎       | 部員が所属する学舎                              | 文字列   | `瀬田`      | `瀬田` or `深草`             |
| D    | (任意)備考 | その他情報                                      | 文字列   |             |                              |


#### 進捗一覧 :
全部員の各問題への進捗状況を記録します。GASによって自動更新されます。

| 列/行     | A列         | B列     | C列     | D列         | E列         |  ...  |
| :-------- | :---------- | :------ | :------ | :---------- | :---------- | :---: |
| **1行目** | (空白)      | (空白)  | (空白)  | 問題ID1     | 問題ID2     |  ...  |
| **2行目** | 学籍番号    | 氏名    | 学舎    | 問題名1     | 問題名2     |  ...  |
| **3行目** | (学籍番号1) | (氏名1) | (学舎1) | `TRUE`/空白 | `TRUE`/空白 |  ...  |
| **4行目** | (学籍番号2) | (氏名2) | (学舎2) | `TRUE`/空白 | `TRUE`/空白 |  ...  |
| **...**   | ...         | ...     | ...     | ...         | ...         |  ...  |


#### GASロジックに関わる設定を行うシート :
GASの動作に必要な設定値を一元管理します。
各シート名や表構成の変更を行う場合、このシートのB列を適宜編集してください。
ヘッダーはありません。

| A列 (キー)                          | B列 (設定値 : 例)            | C列 (設定項目の説明)                         |
| ----------------------------------- | ---------------------------- | -------------------------------------------- |
| `formResponseSheetId`               | `0123456789`                 | フォーム回答シートのID                       |
| `problemMasterSheetId`              | `0123456789`                 | 問題マスターシートのID                       |
| `memberListSheetId`                 | `0123456789`                 | 部員名簿シートのID                           |
| `progressListSheetId`               | `0123456789`                 | 進捗一覧シートのID                           |
| `formSettingsSheetId`               | `0123456789`                 | フォーム設定シートのID                       |
| `formResponseStudentNameColIndex`   | `8`                          | フォーム回答収集シートの氏名記録列番号 (H=8) |
| `formResponseStudentCampusColIndex` | `9`                          | フォーム回答収集シートの記録学舎列番号 (I=9) |
| `progressListProblemIdRowIndex`     | `1`                          | 進捗一覧シートの問題IDの行番号               |
| `progressListProblemNameRowIndex`   | `2`                          | 進捗一覧シートの問題名の行番号               |
| `progressListStudentNumberColIndex` | `1`                          | 進捗一覧シートの学籍番号列番号 (A=1)         |
| `progressListStudentNameColIndex`   | `2`                          | 進捗一覧シートの氏名列番号 (B=2)             |
| `progressListStudentCampusColIndex` | `3`                          | 進捗一覧シートの学舎列番号(C=3)              |
| `progressListProblemStartColIndex`  | `4`                          | 進捗一覧シートの問題開始列番号 (D=4)         |
| `memberListStudentNumberColIndex`   | `1`                          | 部員名簿シートの学籍番号列番号 (A=1)         |
| `memberListStudentNameColIndex`     | `2`                          | 部員名簿シートの氏名列番号 (B=2)             |
| `memberListStudentCampusColIndex`   | `3`                          | 部員名簿シートの学舎列番号 (C=3)             |
| `studentNumberLength`               | `7`                          | メールアドレスから抽出する学籍番号の文字数   |
| `adminEmail`                        | `x000000@mail.ryukoku.ac.jp` | エラー通知先メールアドレス                   |
| `formId`                            | `xxxxxxxxxxxxxxxxxxxxx`      | フォーム更新GASで使用するGoogleフォームのID  |
| ...                                 | ...                          | ...                                          |

#### フォームに関わる設定を行うシート :
フォームのタイトルや質問などを管理します。
各IDはフォームに紐づいた値なので、使用するフォームを変更または再構成しない限りは変更してはいけません。

| A列 (キー)                   | B列 (設定値 : 例)                              | C列 (設定項目の説明)                           |
| ---------------------------- | ---------------------------------------------- | ---------------------------------------------- |
| `formTitle`                  | `練習問題 完了報告フォーム`                    | フォーム全体のタイトル                         |
| `formDescription`            | `完了した練習問題について報告してください。`   | フォーム全体の説明文                           |
| `problemSelectQuestionId`    | `0123456789`                                   | 問題選択の質問のID                             |
| `problemSelectQuestionTitle` | `完了した問題を選択してください`               | 問題選択の質問のタイトル                       |
| `uploadFileSectionId`        | `0123456789`                                   | ファイルをアップロードするセクションのID       |
| `uploadFileSectionTitle`     | `プログラムの提出`                             | ファイルをアップロードするセクションのタイトル |
| `uploadFileQuestionId`       | `0123456789`                                   | ファイルをアップロードする質問のID             |
| `uploadFileQuestionTitle`    | `プログラムのファイルを提出してください`       | ファイルをアップロードする質問のタイトル       |
| `understandingSectionId`     | `0123456789`                                   | 理解度セクションのID                           |
| `understandingSectionTitle`  | `理解度`                                       | 理解度セクションのタイトル                     |
| `understandingQuestionId`    | `0123456789`                                   | 理解度の質問のID                               |
| `understandingQuestionTitle` | `これまでの理解度を教えてください`             | 理解度の質問のタイトル                         |
| `feedbackReqSectionId`       | `0123456789`                                   | 質問（必須）のセクションのID                   |
| `feedbackReqSectionTitle`    | `質問・わからないこと`                         | 質問（必須）のセクションタイトル               |
| `feedbackReqQuestionId`      | `0123456789`                                   | 質問（必須）の質問のID                         |
| `feedbackReqQuestionTitle`   | `理解度が低かった点や疑問点を教えてください`   | 質問（必須）の質問のタイトル                   |
| `feedbackOptSectionId`       | `0123456789`                                   | 質問（任意）のセクションのID                   |
| `feedbackOptSectionTitle`    | `質問・わからないこと (任意)`                  | 質問（任意）のセクションタイトル               |
| `feedbackOptQuestionId`      | `0123456789`                                   | 質問（任意）の質問のID                         |
| `feedbackOptQuestionTitle`   | `質問やわからないことがあれば記入してください` | 質問（任意）の質問のタイトル                   |
| ...                          | ...                                            | ...                                            |


### スクリプト設計

このシステムのでGASは**管理用スプレッドシート**にバインドされています。

各シートに対応するクラスを作成し、データのアクセス権と処理の役割を明確に分けたシステムとします。
各クラスは対応するシート以外の情報取得および更新を行わないことが原則です。
ただし、`設定`および`フォーム設定`シートに関してはクラスを作成せず情報を取得する専用の関数のみを用意します。
スプレッドシートのトリガーから実行される関数（**トリガー関数**）の中で、
各シートの更新などを行う関数（**ハンドラ関数**）を実行します。
場合によっては条件分岐により同じトリガー関数から異なるハンドラ関数が実行されることがあります。
ハンドラ関数内では必要なクラスをインスタンス化し、 各メソッドを呼び出すことでデータの取得や更新を行います。

#### 各種クラス

このシステムのGASには以下のクラスが存在します。
各種クラスの詳細は[こちら]()より。

* **`FormResponseManager`**

	フォームの回答データを管理します。
	主にフォームの回答データの提供と、フォームの回答データへの追加情報付加を行います。

* **`ProblemMasterManager`**

  	問題マスタシートのデータを管理します。
	主に問題名やID、カテゴリなどの読み出しを行います。

* **`NameListManager`**

	部員名簿シートのデータを管理します。
	主に氏名や学舎の読み出しを行います。

* **`ProgressListManager`**

	進捗一覧シートのデータを管理します。
	主に進捗一覧シートへの進捗情報書き込みを行います。

* **`FormManager`**

	進捗収集フォームを管理します。
	主にフォームのタイトルや問題選択肢の更新を行います。


#### 各種関数

このシステムにはクラスに属さない関数がいくつか存在します。
各種関数の詳細は[こちら]()より。

* **`getSettings`**
  
	`設定`シートの設定値を取得します。

* **`getFormSettings`**

	`フォーム設定`シートの設定値を取得します。

* **`onFormSubmit`**

	フォームの回答が送信された際に実行される**トリガー関数**です。
	必要な**ハンドラ関数**を実行します。

* **`onSheetEdit`**

	シートが編集されたときに実行される**トリガー関数**です。
	編集されたシートに応じて**ハンドラ関数**を実行します。

* **`updateProgressList`**

	進捗一覧を更新する**ハンドラ関数**です。
	内部で必要なクラスをインスタンス化し各種メソッドを実行します。

* **`updateProblemName`**

	進捗一覧の問題名を問題マスターシートに従って更新する**ハンドラ関数**です。
	内部で必要なクラスをインスタンス化し各種メソッドを実行します。

* **`updateFormTitles`**

	フォームのタイトルなどを更新する**ハンドラ関数**です。
	内部で必要なクラスをインスタンス化し各種メソッドを実行します。

* **`updateFormSelection`**

	フォームの問題選択肢を更新する**ハンドラ関数**です。
	内部で必要なクラスをインスタンス化し各種メソッドを実行します。